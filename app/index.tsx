import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const HomeScreen = () => {
  return (
    <View className='flex-1 justify-center bg-emerald-300 items-center'>
      <Text className='text-white font-bold text-2xl'>Hello World</Text>
    </View>
  );
};

export default HomeScreen;

const styles = StyleSheet.create({});
